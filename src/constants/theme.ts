import { ThemeConfig } from 'antd';

// ANTD THEME...
export const THEME: ThemeConfig = {
  token: {
    fontFamily: 'Manrope',
  },
};

export { default as COLORS } from '@styles/colors.scss';
